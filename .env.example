# ========================================
# CONFIGURACIÓN OBLIGATORIA
# ========================================

# URL de tu instancia de Jackett (http://localhost:9117 si está en la misma máquina)
JACKETT_URL=http://localhost:9117

# API Key de Jackett (encuéntrala en el dashboard de Jackett)
JACKETT_API_KEY=your_jackett_api_key_here

# ========================================
# CONFIGURACIÓN DE SERVIDOR
# ========================================

# Puerto del servidor (por defecto: 4000)
PORT=4000

# Carpeta para datos persistentes (SQLite cache, torrents)
# IMPORTANTE: En producción NO usar /tmp, usar ruta persistente
DATA_FOLDER=/home/ubuntu/jackettio-data

# Confiar en proxy (nginx, cloudflare, etc.)
TRUST_PROXY=loopback, linklocal, uniquelocal

# ========================================
# SERVICIOS OPCIONALES
# ========================================

# TMDB Access Token (opcional - para metadata en lugar de Cinemeta)
TMDB_ACCESS_TOKEN=

# LocalTunnel (desarrollo - NO usar en producción)
LOCALTUNNEL=false

# ========================================
# PERSONALIZACIÓN DEL ADDON
# ========================================

# ID del addon
ADDON_ID=community.stremio.jackettio

# Nombre del addon
ADDON_NAME=Jackettio

# Descripción
ADDON_DESCRIPTION=Stremio addon that resolve streams using Jackett and Debrid

# URL del icono
ADDON_ICON=https://avatars.githubusercontent.com/u/15383019?s=48&v=4

# ========================================
# SEGURIDAD (PARA TRACKERS PRIVADOS)
# ========================================

# Reemplazar passkey (para instancias públicas con trackers privados)
REPLACE_PASSKEY=
REPLACE_PASSKEY_INFO_URL=
REPLACE_PASSKEY_PATTERN=[a-zA-Z0-9]+

# Claves de configuración inmutables (separadas por coma)
IMMULATABLE_USER_CONFIG_KEYS=

# ========================================
# RATE LIMITING
# ========================================

# Ventana de rate limit en segundos (por defecto: 1 hora)
RATE_LIMIT_WINDOW=3600

# Número máximo de peticiones en la ventana
RATE_LIMIT_REQUEST=150

# ========================================
# OPTIMIZACIÓN Y PERFORMANCE
# ========================================

# Tiempo para considerar indexer como lento (segundos)
SLOW_INDEXER_DURATION=20

# Ventana de monitoreo de indexers lentos (segundos)
SLOW_INDEXER_WINDOW=1800

# Peticiones lentas consecutivas para deshabilitar indexer
SLOW_INDEXER_REQUEST=5

# Timeout de descarga de torrent desde debrid (segundos)
DEBRID_DOWNLOAD_TIMEOUT=10

# Intervalo de polling (milisegundos)
DEBRID_POLLING_INTERVAL=1000

# Máximo de reintentos de polling
DEBRID_MAX_RETRIES=8

# Habilitar chequeo de cache instantáneo
ENABLE_CACHE_CHECK=false

# ========================================
# CONFIGURACIÓN POR DEFECTO DE USUARIOS
# ========================================

# Calidades permitidas (separadas por coma)
DEFAULT_QUALITIES=0,720,1080

# Keywords a excluir (separadas por coma)
DEFAULT_EXCLUDE_KEYWORDS=

# Máximo de torrents a mostrar
DEFAULT_MAX_TORRENTS=8

# Idiomas priorizados (separadas por coma)
DEFAULT_PRIOTIZE_LANGUAGES=

# Priorizar packs de series (0=no, 1=sí, 2=muy alta prioridad)
DEFAULT_PRIOTIZE_PACK_TORRENTS=2

# Forzar cache del siguiente episodio
DEFAULT_FORCE_CACHE_NEXT_EPISODE=false

# Orden para torrents cacheados (campo:ascendente)
DEFAULT_SORT_CACHED=quality:true,size:true

# Orden para torrents no cacheados
DEFAULT_SORT_UNCACHED=seeders:true

# Ocultar torrents no cacheados
DEFAULT_HIDE_UNCACHED=false

# Indexers por defecto (all, o lista separada por comas)
DEFAULT_INDEXERS=all

# Timeout de indexers en segundos
DEFAULT_INDEXER_TIMEOUT_SEC=60

# Idioma de metadata (ISO 639-1, ejemplo: en, es, fr)
DEFAULT_META_LANGUAGE=

# ========================================
# MEDIAFLOW PROXY (OPCIONAL)
# ========================================

# Habilitar MediaFlow para streaming
DEFAULT_ENABLE_MEDIA_FLOW=false

# URL del proxy MediaFlow
DEFAULT_MEDIA_FLOW_PROXY_URL=

# Password API de MediaFlow
DEFAULT_MEDIA_FLOW_API_PASSWORD=

# IP pública de MediaFlow
DEFAULT_MEDIA_FLOW_PUBLIC_IP=

# ========================================
# CLOUDFLARE (OPCIONAL)
# ========================================

# Confiar en header cf-connecting-ip
TRUST_CF_IP_HEADER=false

# ========================================
# MENSAJE DE BIENVENIDA
# ========================================

# Mensaje personalizado en página de configuración (Markdown)
WELCOME_MESSAGE=
